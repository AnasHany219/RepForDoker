on:
  push:
    branches:
      - master
    
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: "Login to Docker hun"
        run: docker login --username anashany219  --password dckr_pat_Qk0gFzMyiYv06FdBPgXj1NPCGPs